# Audio utilities (actions/audio_utils)
pydub
faster-whisper
pyannote.audio==4.0.4  # torchcodec ABI broken; io.py patched to use soundfile instead
torch
torchaudio

# Picture utilities (actions/picture_utils)
Pillow>=10.0.0
rawpy==0.25.1
imageio==2.31.2
imagehash==4.3.1  # perceptual hashing — image_dedup.py --method perceptual
flask==3.1.0      # review_server.py — UI revue near-duplicates

# Document utilities (actions/document_utils)
PyPDF2
python-pptx
reportlab
PyMuPDF
pypandoc
# md_to_audio.py
kokoro
soundfile
numpy

# Shared
python-dotenv

# MinerU PDF → Markdown (actions/document_utils/pdf_mineru_to_md.py)
# Heavy install (PyTorch + ~3 GB of model weights on first use).
mineru[core]